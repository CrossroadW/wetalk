---
name: update-claude-md
description: Update CLAUDE.md after significant project changes - only for major updates to dependencies, CMake config, or compiler versions
disable-model-invocation: true
allowed-tools: Read, Edit, Write, Bash
---

# Update CLAUDE.md

更新 CLAUDE.md 文件以反映项目的当前状态。当项目结构或依赖有重大变化时运行此任务。

## 何时运行此任务

在以下情况时运行此任务：
- ✅ 添加或更新了项目依赖
- ✅ 修改了 CMakeLists.txt 配置
- ✅ 更新了编译选项或编译器版本
- ✅ 改变了项目结构
- ✅ 修改了 VSCode 任务或构建预设

## 更新步骤

1. **读取当前配置文件**：
   - `conan/debug/conanfile.txt` - 检查 Debug 依赖
   - `conan/release/conanfile.txt` - 检查 Release 依赖
   - `CMakeLists.txt` - 获取 CMake 配置信息
   - `CMakePresets.json` - 验证构建预设

2. **提取关键信息**：
   - 依赖名称和版本（从 `[requires]` 部分）
   - C++ 标准版本（从 CMakeLists.txt）
   - 编译器类型和版本
   - Build types (Debug/Release)
   - 最后更新时间戳

3. **更新 CLAUDE.md 文件**：
   - 更新 "最后更新时间" 部分为当前日期时间
   - 更新 "项目依赖" 部分的 Debug 和 Release 配置
   - 保持项目结构部分一致
   - 保持其他参考信息准确

4. **验证更新**：
   - 确保所有依赖信息准确
   - 检查格式是否正确
   - 验证快速命令表是否完整

## CLAUDE.md 文件结构

```
# 项目概览 - CLAUDE.md
│
├── 📋 最后更新时间
│   └── YYYY-MM-DD HH:MM
│
├── 📁 项目结构
│   └── 目录树和文件说明
│
├── 📦 项目依赖
│   ├── Debug 配置
│   │   ├── 依赖列表
│   │   ├── build_type
│   │   └── compiler 信息
│   │
│   └── Release 配置
│       ├── 依赖列表
│       ├── build_type
│       └── compiler 信息
│
├── 🔧 构建工具版本
│   ├── CMake 版本
│   ├── Conan 版本
│   ├── Generator
│   └── C++ Standard
│
├── 🚀 快速命令
│   └── 常用命令表
│
└── 📝 重要文件说明
    └── 关键文件解释
```

## 示例更新

如果你向 `conan/debug/conanfile.txt` 添加了新的依赖：

```diff
[requires]
fmt/10.1.1
+ nlohmann_json/3.11.2
```

运行此任务后，CLAUDE.md 应该更新为：

```markdown
### Debug 配置
- **fmt**: 10.1.1
- **nlohmann_json**: 3.11.2
- **build_type**: Debug
- **compiler**: gcc 11
```

## 自动化建议

在开发工作流中定期运行此任务，以保持文档与代码同步。建议：

1. **在添加依赖后立即运行**
2. **在修改 CMake 配置后运行**
3. **在更新编译器或 C++ 标准版本后运行**
4. **在推送重大更改前运行一次**

## 相关文件

- [CLAUDE.md](../../CLAUDE.md) - 要更新的文件
- [conan/debug/conanfile.txt](../../conan/debug/conanfile.txt) - Debug 依赖
- [conan/release/conanfile.txt](../../conan/release/conanfile.txt) - Release 依赖
- [CMakeLists.txt](../../CMakeLists.txt) - CMake 配置
- [CMakePresets.json](../../CMakePresets.json) - 构建预设
